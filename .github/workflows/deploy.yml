name: Build & Deploy

# push 到 main 时云端交叉编译并 SSH 部署到板端（经 Cloudflare Tunnel）
on:
  push:
    branches: [main]
  workflow_dispatch:

jobs:
  build:
    name: Build arm64 binary
    runs-on: ubuntu-latest
    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Set up Go
        uses: actions/setup-go@v5
        with:
          go-version: '1.23'

      - name: Vet & Test
        run: |
          go vet ./...
          go test ./...

      - name: Cross-compile for arm64
        run: |
          CGO_ENABLED=0 GOOS=linux GOARCH=arm64 go build -trimpath -ldflags "-s -w" -o wol-web ./cmd/wol-web
          file wol-web
          chmod +x wol-web

      - name: Upload binary
        uses: actions/upload-artifact@v4
        with:
          name: wol-web
          path: wol-web
          retention-days: 1

  deploy:
    name: Deploy to board
    needs: build
    runs-on: ubuntu-latest
    steps:
      - name: Download binary
        uses: actions/download-artifact@v4
        with:
          name: wol-web
          path: .

      - name: Install cloudflared
        run: |
          curl -fsSL https://github.com/cloudflare/cloudflared/releases/latest/download/cloudflared-linux-amd64 -o /usr/local/bin/cloudflared
          chmod +x /usr/local/bin/cloudflared
          cloudflared --version

      - name: Write board SSH key
        run: |
          echo "$BOARD_SSH_KEY" > /tmp/board_key
          chmod 600 /tmp/board_key
        env:
          BOARD_SSH_KEY: ${{ secrets.BOARD_SSH_KEY }}

      - name: Copy binary and restart via Cloudflare Tunnel SSH
        run: |
          set -e
          CF_ID="${CLOUDFLARED_TOKEN%%:*}"
          CF_SECRET="${CLOUDFLARED_TOKEN#*:}"

          # 启动 cloudflared access tcp 转发
          cloudflared access tcp \
            --hostname ssh.632-8nm.cloud \
            --url localhost:2222 \
            --service-token-id "$CF_ID" \
            --service-token-secret "$CF_SECRET" \
            --log-level warn > /tmp/cf-tcp.log 2>&1 &
          CF_PID=$!
          trap "kill $CF_PID 2>/dev/null || true" EXIT

          # 等待隧道就绪（SSH 重试）
          echo "==> 等待隧道就绪"
          SSH_OK=0
          for i in $(seq 1 15); do
            if ssh -i /tmp/board_key -o BatchMode=yes -o StrictHostKeyChecking=accept-new \
                -o ConnectTimeout=10 -p 2222 orangepi@127.0.0.1 'true' 2>/dev/null; then
              SSH_OK=1
              break
            fi
            sleep 1
          done
          if [ "$SSH_OK" != "1" ]; then
            echo "错误: 无法通过隧道连接板子，cloudflared 日志:"
            cat /tmp/cf-tcp.log
            exit 1
          fi

          DEPLOY_DIR=/opt/wol-web

          echo "==> 先停止 wol-web 服务（释放二进制占用）"
          ssh -i /tmp/board_key -o BatchMode=yes -o StrictHostKeyChecking=accept-new \
              -o ConnectTimeout=15 -p 2222 orangepi@127.0.0.1 "
            set -e
            sudo systemctl stop wol-web || true
            echo 'wol-web 已停止'
          "

          echo "==> 传输 wol-web 二进制到板子"
          scp -i /tmp/board_key -o BatchMode=yes -o StrictHostKeyChecking=accept-new \
              -o ConnectTimeout=15 -P 2222 wol-web orangepi@127.0.0.1:$DEPLOY_DIR/

          echo "==> SSH 启动 wol-web 服务"
          ssh -i /tmp/board_key -o BatchMode=yes -o StrictHostKeyChecking=accept-new \
              -o ConnectTimeout=15 -p 2222 orangepi@127.0.0.1 "
            set -e
            echo '==> 替换二进制'
            chmod +x $DEPLOY_DIR/wol-web
            sudo systemctl start wol-web
            sleep 2
            echo '==> 服务状态: '\$(systemctl is-active wol-web)
            sudo systemctl is-active wol-web
          "
        env:
          CLOUDFLARED_TOKEN: ${{ secrets.CLOUDFLARED_TOKEN }}
